<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width new code, initial-scale=1.0">
  <title>New Code</title>
</head>

<body>
  <%= form_with(model: code, local: true) do |form| %>
  <% if code.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(code.errors.count, "error") %> prohibited this code from being saved:</h2>
    <ul>
      <% code.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
  </div>
  <% end %>
  <div class="card bg-light">
    <div class="row">
      <div class="col-md-5 order-md-1">
        <%= image_tag("pollo.jpg", style: "width: 500px;")%>
      </div>
      <div class="col-lg-6">
        <div class="p-5">
          <div class="text-center">
            <h1 class="text-gray-900 mb-4">Bienvenido!</h1>
          </div>
          <div class="field">
            <h1 class="font-weight-bold"><%= form.label :code %></h1>
            <%= form.text_field :code, class:"form-control form-control-user" %>
          </div>
          <div class="field">
            <h1 class="font-weight-bold"><%= form.label :description %></h1>
            <%= form.text_area :description, class:"form-control form-control-user" %>
          </div>
          <br>
          <br>
          <div class="actions">
            <%= form.submit class:"btn btn-primary btn-user btn-block"%>
          </div>
          <% end %>
          <br>
          <br>
          <br>
          <%= link_to 'Back', codes_path, class:"btn btn-primary btn-user btn-block" %>
        </div>
      </div>
    </div>
  </div>
</body>